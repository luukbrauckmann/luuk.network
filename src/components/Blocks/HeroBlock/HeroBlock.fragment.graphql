fragment HeroBlock on HeroBlockRecord {
    image {
        responsiveImage(imgixParams: {auto: format}) {
            alt
            aspectRatio
            base64
            bgColor
            height
            sizes
            src
            title
            width
        }
    }
    title
    description
}
