fragment HeroBlock on HeroBlockRecord {
    image {
        responsiveImage(imgixParams: {auto: format}) {
            alt
            aspectRatio
            base64
            bgColor
            height
            sizes
            src
            srcSet
            title
            webpSrcSet
            width
        }
    }
    title
    description
}
