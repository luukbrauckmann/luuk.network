#import '@components/Blocks/ActionBlock/ActionBlock.fragment.graphql'

fragment TextBlock on TextBlockRecord {
    text {
        value
        blocks {
            id
            __typename
            ...ActionBlock
        }
    }
}
